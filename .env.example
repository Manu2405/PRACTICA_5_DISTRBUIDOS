# Cassandra (driver en backend)
CASSANDRA_CONTACT_POINTS=cassandra
CASSANDRA_KEYSPACE=semapa
CASSANDRA_PORT=9042

# API Go (Gin)
GIN_MODE=release
PORT=8000

# MinIO (S3 compatible)
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minio
MINIO_SECRET_KEY=minio12345
MINIO_BUCKET=semapa
